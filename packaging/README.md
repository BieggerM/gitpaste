# Packaging roadmap

The initial release is intentionally limited to GitHub Release archives and checksums. Homebrew tap metadata, an optional Oh My Zsh wrapper, and nfpm-generated `.deb`/`.rpm` packages can be added after the binary-and-setup flow has stabilized.
